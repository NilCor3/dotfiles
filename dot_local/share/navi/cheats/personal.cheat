% git, log

# Show commit log graph  [gl]
gl

# Show commits by author
git log --oneline --author="<author>"

# Show changes in a commit
git show <commit>

# Search commit messages
git log --oneline --grep="<query>"

# Show file history
git log --oneline --follow -- <file>

% git, branches

# List all branches including remote  [gb]
gb -a

# Switch to branch  [gsw]
gsw <branch>

# Create and switch to new branch  [gsw]
gsw -c <branch>

# Delete local branch  [gbd]
gbd <branch>

# Delete remote branch  [gp]
gp origin --delete <branch>

# Rename current branch  [gb]
gb -m <new_name>

% git, staging

# Stage all changes  [gaa]
gaa

# Stage interactively hunk by hunk  [ga]
ga -p

# Unstage a file
git restore --staged <file>

# Discard changes in working directory
git restore <file>

# Show staged diff  [gds]
gds

% git, commits

# Commit with message  [gcm]
gcm "<message>"

# Amend last commit without changing message  [gca]
gca

# Amend last commit message  [gc]
gc --amend -m "<message>"

# Undo last commit keep changes staged  [gc]
gc --soft HEAD~1

# Create fixup commit for a specific commit  [gc]
gc --fixup <commit>

# AI commit: generate message from staged diff (via ollama in tmux popup)
# keybind: Ctrl+Space G  (tmux leader + G)

# AI commit: run script directly
~/.config/tmux/scripts/ai-commit.sh

% git, remote

# Push  [gp]
gp

# Push and set upstream  [gp]
gp -u origin <branch>

# Force push safely  [gpf]
gpf

# Pull with rebase  [gpl]
gpl

# Fetch all remotes and prune deleted branches
git fetch --all --prune

% git, stash

# Stash with description  [gst]
gst push -m "<message>"

# List stashes  [gst]
gst list

# Apply specific stash  [gst]
gst apply stash@{<index>}

# Pop latest stash  [gstp]
gstp

# Drop a stash  [gst]
gst drop stash@{<index>}

% git, rebase

# Interactive rebase on last N commits  [grbi]
grbi HEAD~<n>

# Interactive rebase on main  [grbi]
grbi origin/main

# Autosquash fixup commits  [grbi]
grbi --autosquash origin/main

# Abort rebase  [grb]
grb --abort

# Continue rebase after resolving conflicts  [grb]
grb --continue

% git, merge conflicts

# Solve conflict markers in a file (mergiraf post-process)
mergiraf solve <file>

# Review how mergiraf resolved a merge vs line-based
mergiraf review <file>

# Solve all conflicted files in repo
git diff --name-only --diff-filter=U | xargs -I{} mergiraf solve {}

% git, misc

# Open lazygit  [lg]
lg

# Cherry-pick a commit
git cherry-pick <commit>

# Show who changed a line
git blame <file>

# Clean untracked files dry run
git clean -nd

# Clean untracked files
git clean -fd

# Get a single file from a branch without checking out
git show <branch>:<path/to/file> > <output_file>

# Get all files from a branch into working tree without checking out
git checkout origin/<branch> -- .

# Bring all changes from a branch as a single staged diff (no commits)
git merge --squash origin/<branch>

% go, build & run

# Run current module
go run .

# Build all packages  [gob]
gob

# Build with race detector
go build -race ./...

# Cross-compile for Linux
GOOS=linux GOARCH=amd64 go build -o <output> ./...

% go, testing

# Run all tests verbose  [gota]
gota

# Run all tests in specific package  [gota]
gota ./<package>/...

# Run tests matching name partial or full  [got]
got <name>

# Run tests matching name in specific package  [got]
got <name> ./<package>/...

# Run with race detector
go test -race ./...

# Run with coverage report
go test -cover ./...

# Generate html coverage report
go test -coverprofile=coverage.out ./... && go tool cover -html=coverage.out

# Clear test cache  [gocc]
gocc

% go, modules

# Initialize a new module
go mod init <module>

# Add missing and remove unused modules  [gom]
gom

# Upgrade a dependency
go get <module>@latest

# Upgrade all dependencies
go get -u ./...

% go, tools

# Run linter  [gol]
gol

# Run linter with auto-fix
golangci-lint run --fix

# Generate code  [gog]
gog

# Vet code
go vet ./...

% rust, build & run

# Build debug  [cb]
cb

# Build release  [cb]
cb --release

# Run  [cr]
cr

# Run with arguments  [cr]
cr -- <args>

# Run release  [cr]
cr --release

% rust, testing

# Run all tests  [ct]
ct

# Run tests matching name  [ct]
ct <name>

# Run tests with output  [ctv]
ctv

# Run specific test with output  [ctv]
ctv <name> -- --nocapture

# Run with all features  [ct]
ct --all-features

% rust, code quality

# Check without building  [cc]
cc

# Run clippy  [ccl]
ccl

# Run clippy auto-fix  [cclf]
cclf

# Format code  [cf]
cf

# Format all  [cfa]
cfa

# Format check only (CI)  [cf]
cf -- --check

% rust, dependencies

# Add a dependency
cargo add <crate>

# Add a dev dependency
cargo add --dev <crate>

# Update all dependencies
cargo update

# Show dependency tree
cargo tree

% rust, misc

# Generate and open docs  [cdoc]
cdoc

# Clean build artifacts
cargo clean

% npm, packages

# Install dependencies
npm install

# Add a dependency
npm install <package>

# Add a dev dependency
npm install --save-dev <package>

# Remove a package
npm uninstall <package>

# Show outdated packages
npm outdated

# Audit for vulnerabilities
npm audit

# Fix audit issues
npm audit fix

% npm, scripts

# Run a script
npm run <script>

# List available scripts
npm run

# Run tests
npm test

# Run build
npm run build

# Run dev server
npm run dev

% chezmoi, daily

# Check what has changed home vs source
chezmoi diff

# Pull home dir changes back into source
chezmoi re-add

# Apply source to home dir
chezmoi apply

# Add a new file to chezmoi
chezmoi add <file>

# Show managed files
chezmoi managed

# Show status of all managed files
chezmoi status

# Edit a managed file in source
chezmoi edit <file>

% chezmoi, git

# Show git log of dotfiles repo
chezmoi git -- log --oneline

# Push dotfiles
chezmoi git -- push

# Pull latest dotfiles from remote
chezmoi update

% chezmoi, secrets & misc

# Force re-run of a run_once script
chezmoi state delete-bucket --bucket=scriptState

# Open source directory
chezmoi cd

% xh, http requests

# GET request
xh <url>

# GET with custom header
xh <url> <Header>:<value>

# POST JSON (string=value, number:=value)
xh POST <url> key=value key2:=123

# POST with Bearer token
xh <url> Authorization:"Bearer <token>"

# Print only response body (pipe-friendly)
xh --print=b <url>

# Download a file
xh --download <url>

# Follow redirects
xh --follow <url>

# Preview request without sending
xh --offline POST <url> key=value

# Persistent session (persists cookies/headers)
xh --session=<name> <url>

# Pipe response through jq
xh --print=b <url> | jq '<filter>'

% eza, files

# Detailed list with icons and git status  [ll]
eza -lah --icons --git

# Tree view N levels deep  [lt]
eza --tree --icons --git -L <levels>

# Sort by modified time newest first
eza -lah --icons --git --sort=modified --reverse

# Show only directories
eza -lah --icons --only-dirs

# Group directories first
eza -lah --icons --group-directories-first

% pgcli, postgres

# Connect to local database
pgcli <database>

# Connect with user and host
pgcli -h <host> -U <user> <database>

# Connect via URL
pgcli postgresql://<user>:<password>@<host>/<database>

# Inside pgcli: describe a table
\d <table>

# Inside pgcli: list all tables
\dt

# Inside pgcli: list databases
\l

# Inside pgcli: save a named query
\ns <name> SELECT * FROM <table> WHERE <condition>

# Inside pgcli: run a named query
\n <name>

# Inside pgcli: export query result to CSV
\copy (SELECT * FROM <table>) TO '<file>.csv' CSV HEADER

% http, mock, prism, json-server

# Mock from OpenAPI spec (local file)
prism mock <openapi.yaml>

# Mock from OpenAPI spec (remote URL)
prism mock <https://api.example.com/openapi.yaml>

# Mock with verbose logging
prism mock --verbose <openapi.yaml>

# Proxy to real API + validate requests/responses
prism proxy <openapi.yaml> <https://real-api.example.com>

# Start json-server from a db.json file
json-server <db.json>

# json-server with custom port
json-server --port <port> <db.json>

# json-server with custom routes file
json-server --routes <routes.json> <db.json>

% go, tools, air, sqlc, gotestsum, dlv, oapi-codegen

# Run gotestsum (pretty test output)
gotestsum

# Watch mode - rerun tests on save
gotestsum --watch

# Show individual test names
gotestsum --format testname ./...

# Initialize air config in project
air init

# Start air live reload server
air

# Generate SQL code from schema + queries
sqlc generate

# Vet SQL queries for errors
sqlc vet

# Init new sqlc project
sqlc init

# Start delve debugger on current package
dlv debug

# Attach delve to running process
dlv attach <pid>

# Start delve DAP server (for editor integration)
dlv dap --listen=:2345

# Debug with gdlv GUI + dlv CLI companion (both connected to same session)
dlvg <subcommand> <package>

# Start headless dlv server for multi-client (CLI + gdlv)
dlv <subcommand> --headless --listen=:2345 --accept-multiclient <package>

# Connect gdlv GUI to headless dlv server
gdlv connect :2345

# Connect dlv CLI to headless dlv server
dlv connect :2345

# Generate Go server/client from OpenAPI spec
oapi-codegen -generate types,server -package api openapi.yaml > api/api.gen.go

# Generate only types
oapi-codegen -generate types -package api openapi.yaml > api/types.gen.go

% rust, cargo, bacon, nextest, audit, expand

# Start bacon (background test/check watcher)
bacon

# bacon check mode (no test run)
bacon check

# bacon clippy mode
bacon clippy

# Run tests with nextest
cargo nextest run

# Run a specific test with nextest
cargo nextest run <test_name>

# Audit dependencies for vulnerabilities
cargo audit

# Fix vulnerable dependencies automatically
cargo audit fix

# Expand macros for a module
cargo expand <module>

# Expand the whole crate
cargo expand

% json, jq, fx, jnv

# Interactive JSON explorer (pipe)
<command> | fx

# Open JSON file in fx
fx <file.json>

# fx: JS expression filter
fx <file.json> '.<field>'

# Interactive jq filter builder (pipe)
<command> | jnv

# Open JSON file in jnv
jnv <file.json>

# jq: pretty print
jq . <file.json>

# jq: extract field
jq '.<field>' <file.json>

# jq: filter array
jq '.[] | select(.<field> == "<value>")' <file.json>

# jq: extract keys
jq 'keys' <file.json>

# jq: map over array
jq '[.[] | {name: .<name_field>, id: .<id_field>}]' <file.json>

# jq: compact output (no pretty print)
jq -c '.' <file.json>

# jq: raw string output (no quotes)
jq -r '.<field>' <file.json>

# jq: count array elements
jq 'length' <file.json>

# jq: sort by field
jq 'sort_by(.<field>)' <file.json>

# jq: group by field
jq 'group_by(.<field>)' <file.json>

# Pipe xh response into jnv for exploration
xh <url> | jnv

# Pipe xh response into fx for scripting
xh <url> | fx '.<field>'

% notes, markdown, glow, marksman

# Open notes folder in Helix
n

# Open today's journal entry
nj

# New meeting note
nmeet <title>

# New issue note
nissue <NOV-123>

# New note at path (relative to ~/notes)
nn <work/my-note.md>

# Open scratch.md (quick capture)
ns

# Ripgrep search → fzf file picker with match preview
ng <pattern>

# Live interactive content search (type to filter, opens hx at exact line)
ngs

# Browse notes with glow TUI
ngl

# Fuzzy find notes by filename
nf <query>

# Filter notes by frontmatter type (meeting, issue, daily, ref, system)
nft <type>

# Filter notes by tag
ntag <tag>

# Render a single markdown file
glow <file.md>

# Sync notes to git remote (commit + push)
nsync

# Review current note with local AI (structure, gaps, clarity)
nreview

# Review a specific note with local AI
nreview <path/to/note.md>

% ai, ollama, mods

# Start ollama as background launchd service (auto-starts on login)
launchctl load ~/Library/LaunchAgents/com.ollama.serve.plist

# Stop ollama background service
launchctl unload ~/Library/LaunchAgents/com.ollama.serve.plist

# Check if ollama service is running
curl -s http://localhost:11434

# View ollama service logs
tail -f /tmp/ollama.log

# Start ollama server (foreground / manual)
ollama serve

# Pull a model
ollama pull <gemma3:4b>

# List installed models
ollama list

# Chat with local model interactively
ollama run <gemma3:4b>

# Pipe to local AI (general)
<command> | mods -a ollama -m gemma3:4b "<prompt>"

# Pipe to local coder model
<command> | mods -a ollama -m qwen2.5-coder:7b "<prompt>"

# Explain code
cat <file> | ai "explain this code"

# Write a commit message from diff
git diff | ai "write a concise commit message"

# Review code for issues
cat <file> | aic "review this for bugs and improvements"

# Summarize a file
cat <file> | m "summarize this in 3 bullet points"

# Ask a question (no pipe)
ai "what is the difference between INNER JOIN and LEFT JOIN"

% typescript, biome, tsx, knip

# Format and lint fix
biome check --apply

# Format only
biome format --write <path>

# Lint check (no fix)
biome check <path>

# Run TypeScript file directly
tsx <file.ts>

# Find unused exports, deps, files
knip

% slides, presentations

# Start a slideshow from markdown
slides <presentation.md>


% helix, build

# Update Helix to latest upstream (fetch, rebase, build, deploy)
cd ~/hx/source && git fetch upstream && git rebase upstream/master && cargo build --release && cd ~/hx && bash setup_build.sh

# Build and deploy Helix (after code change)
cd ~/hx/source && cargo build --release && cd ~/hx && bash setup_build.sh

# Authenticate Copilot in Helix (first time or after token expiry)
python3 ~/hx/copilot_auth.py


# Toggle inline AI provider between Copilot and ollama (requires hx restart)
bash ~/.config/helix/scripts/hx-toggle-ai.sh && echo "Restart hx to apply"

# Check current AI provider
cat ~/.config/helix/.ai-provider

# View ollama-ls debug log
tail -f /tmp/ollama-ls.log


% helix, files

# Open yazi file manager in popup at current buffer's dir, open chosen files
# keybind: Space , y

# Open built-in file explorer sidebar
# keybind: Ctrl+e


% wezterm, scroll, keybindings

# Scroll view down 1 line — works without entering scroll mode
# Keybind: CTRL+SHIFT+j

# Scroll view up 1 line — works without entering scroll mode
# Keybind: CTRL+SHIFT+k

# Scroll half page down — works without entering scroll mode
# Keybind: CTRL+SHIFT+d

# Scroll half page up — works without entering scroll mode
# Keybind: CTRL+SHIFT+u

# Enter scroll mode (j/k lines, d/u half-page, g/G top/bottom, v → copy, / → search)
# Keybind: LEADER y  (CTRL+SHIFT+Space, then y)

# Enter copy mode (hjkl, w/e/b words, v visual, V line, x select-line, y yank, / search)
# Keybind: LEADER c  (CTRL+SHIFT+Space, then c)

% wezterm, pane, keybindings

# Navigate pane left (mirrors Helix CTRL+w h)
# Keybind: LEADER h  (CTRL+SHIFT+Space, then h)

# Navigate pane down (mirrors Helix CTRL+w j)
# Keybind: LEADER j  (CTRL+SHIFT+Space, then j)

# Navigate pane up (mirrors Helix CTRL+w k)
# Keybind: LEADER k  (CTRL+SHIFT+Space, then k)

# Navigate pane right (mirrors Helix CTRL+w l)
# Keybind: LEADER l  (CTRL+SHIFT+Space, then l)

# Resize pane left
# Keybind: ALT+h

# Resize pane down
# Keybind: ALT+j

# Resize pane up
# Keybind: ALT+k

# Resize pane right
# Keybind: ALT+l

# Zoom pane (toggle fullscreen for current pane)
# Keybind: LEADER z  (CTRL+SHIFT+Space, then z)

# Collapse current pane to 1 row/col (focus moves to neighbor)
# Keybind: LEADER a  (CTRL+SHIFT+Space, then a)

# Restore collapsed pane (navigate back to it first, then press)
# Keybind: LEADER a  (CTRL+SHIFT+Space, then a)


% zellij, multiplexer

# Start Zellij (runs inside WezTerm)
zellij

# Attach to existing session
zellij attach <session>

# List sessions
zellij list-sessions

# Detach from session
# Keybind: Ctrl+\ then g, then d

# Navigate panes (mirrors Helix Ctrl+w h/j/k/l)
# Keybind: Ctrl+\ then h/j/k/l

# New vertical split / horizontal split
# Keybind: Ctrl+\ then v (vsplit) or s (hsplit)

# Close focused pane
# Keybind: Ctrl+\ then q

# Zoom pane (fullscreen toggle)
# Keybind: Ctrl+\ then z

# Toggle floating panes (show/hide)
# Keybind: Ctrl+\ then f

# New floating pane
# Keybind: Ctrl+\ then F

# New tab / close tab
# Keybind: Ctrl+\ then n / x

# Go to tab by number
# Keybind: Ctrl+\ then 1-9

# Enter resize mode (h/j/k/l to resize, Esc to exit)
# Keybind: Ctrl+\ then R

# Session picker
# Keybind: Ctrl+\ then g

% todos, notes

# Add todo to inbox
ta <text>

# Add todo to inbox (open inbox in helix)
ta

# Insert todo checkbox below cursor in Helix (markdown files only)
# keybind: Alt+t in normal mode

# List all open todos (fzf, opens in helix)
tl

# Mark a todo as done (fzf picker)
td

# Open a todo project file (fzf)
tp

# Open a specific todo project file
tp <project>

# Filter todos by tag (fzf, opens in helix)
tg <tag>
